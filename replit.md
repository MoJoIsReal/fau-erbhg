# FAU Erdal Barnehage

Replit's project notes for this repository.

The architecture, conventions, commands and agent guidance for this project are
maintained in [`AGENTS.md`](AGENTS.md) at the repository root (Claude Code reads
it via `CLAUDE.md`). Detailed documentation lives in [`docs/`](docs/README.md).
Keep those files as the single source of truth rather than duplicating them here.

Run configuration for Replit itself is in `.replit`; the dev server is
`npm run dev` on port 5000.
